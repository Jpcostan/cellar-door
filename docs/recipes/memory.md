# Memory Recipes

Add memory:
```bash
cellar-door memory add "Remember to rotate keys" --tags security,keys --type lesson
```

Search memory:
```bash
cellar-door memory search "rotate keys"
```

Compact memory:
```bash
cellar-door memory compact
```

